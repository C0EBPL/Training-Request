/* css/styles.css */
body {
  font-family: Arial, sans-serif;
  padding: 2rem;
}

form {
  max-width: 400px;
  margin: auto;
}

input, select {
  width: 100%;
  margin-bottom: 1rem;
  padding: 0.5rem;
}
